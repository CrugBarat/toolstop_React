import React, {Fragment} from 'react';
import HelpBar from './helpBar/HelpBar.js'

const Header = (props) => {
  return (
    <Fragment>
      <HelpBar />
    </Fragment>
  )
}

export default Header;
